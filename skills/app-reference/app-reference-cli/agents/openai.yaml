interface:
  display_name: "CLI App Reference"
  short_description: "Terminal tool form, input placement, output, and long-running work"
  default_prompt: "Use $app-reference-cli to choose and implement an appropriate terminal tool interface."
