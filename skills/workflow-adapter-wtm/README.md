# wtm workflow adapter

Provider integration for using the externally installed `wtm-worktree` skill
through generic workflows in this repository.

| Skill | Description |
|---|---|
| `workflow-adapter-wtm` | Apply provider-neutral worktree operations through wtm |

Install `wtm-worktree` separately. This group owns the integration boundary;
that skill owns the wtm CLI model and command mechanics.
