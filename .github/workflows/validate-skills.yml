name: Validate skills

on:
  push:
    paths:
      - "skills/**"
      - "scripts/validate-skills.sh"
      - "Makefile"
      - "lefthook.yml"
      - ".github/workflows/validate-skills.yml"
  pull_request:
    paths:
      - "skills/**"
      - "scripts/validate-skills.sh"
      - "Makefile"
      - "lefthook.yml"
      - ".github/workflows/validate-skills.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"
          cache: false

      - name: Check out skill-validator fork
        uses: actions/checkout@v4
        with:
          repository: choplin/skill-validator
          path: .skill-validator

      - name: Install skill-validator fork
        working-directory: .skill-validator
        run: go install ./cmd/skill-validator

      - name: Validate skills
        run: scripts/validate-skills.sh --emit-annotations
